# Upstream prompt snapshot

The YAML files in this directory are copied from the Role Confusion upstream
repository at commit `ec333c40fd43fe991e1ebf66765051b6d7e35784`. Their source
paths and SHA-256 hashes are recorded in `SOURCE_MANIFEST.yaml`. The upstream
repository identifies the snapshots as MIT-licensed; the complete upstream
notice is copied in `LICENSE-MIT-upstream.md`.

These files are retained as research prompt provenance. This repository
intentionally does not include generated harmful request rows or model
outputs. Verify any separate dataset access terms before redistribution.
